requests>=2.31
python-dotenv>=1.0
flask>=3.0
gunicorn>=21.2
tifffile>=2024.1
imagecodecs>=2024.1  # tifffile's own compressed-TIFF decoder plugin — needed for Copernicus GLO-30's floating-point-predictor COGs (confirmed live: plain tifffile alone raises "PREDICTOR.FLOATINGPOINT requires the 'imagecodecs' package" on a real downloaded tile); OPW/TII/Westmeath LIDAR tiles are uncompressed and never needed this
numpy>=1.26
pyproj>=3.6
Pillow>=10.0
shapely>=2.1  # needs constrained_delaunay_triangles() — confirmed present in 2.1.2, not verified further back
scipy>=1.11  # spatial.cKDTree — the standard, exact tool for nearby-point lookups, used for the inverse-distance-weighted, smoothly-fading correction that blends Copernicus GLO-30 fill toward real LIDAR at the seam (see elevation.py's _seam_blend_fill()); not hand-rolled since a real verified algorithm/data structure exists for exactly this
